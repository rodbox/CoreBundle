<div class="btn-group"><label for="{{ id }}" class="btn btn-opacity {% if checked|default(false) %}active{% endif %}"><input data-url="{{ url }}" {% if checked|default(false) %}checked{% endif %} type="checkbox" value="true" class="toggle-me custom-control-input" name="{{ id }}[]"  id="{{ id }}" autocomplete="off"/><span class="custom-control-indicator"></span> <span class="custom-control-description">{{ content|raw}}</span></label></div>